Ballooning Mission Control Center
=================================

Run server locally (requires local proxy):

    docker compose run web


Add new python dependencies:

    docker compose deps add '<package name>'
